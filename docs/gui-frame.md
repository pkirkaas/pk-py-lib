A couple more requirements

## some sort of flexible debug logging - 
Not just for errors, but also just to output values and variables as we go along.

It should be possible to log/debug to several outputs -
to the terminal, to a log file, to some sort of output pane in the GUI, and eventually to a DB - not necessary now, but enabled.

## A GUI APP test frame

This will be a library of exported components, not a full application itself, but I would like to have some sort of main GUI window where I could load the GUI components to see how the look and work

I don't have a strong opinion the best way to implement these - please be creative and come up with some good, flexible suggestions