---
description: "Run imgapp in terminal"
---

Run the terminal command `pdm run imgapp` and monitor the output for errors, info, etc.

Use the output of the command to determine success, errors, or next steps